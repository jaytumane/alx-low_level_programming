#include <stdio.h>

/**
 * main - makes an infinity loop
 *
 * Return: 0 Always (success)
 */
int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");
	i = 0;

	/**
	 *while (i < 10)
	 *{
	 *	putchar(i);
	 *}
	 */

	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
